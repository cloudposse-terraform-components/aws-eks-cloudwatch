components:
  terraform:
    eks/cloudwatch:
      metadata:
        component: eks/cloudwatch
      vars:
        enabled: true
        # required for testing
        kube_exec_auth_role_arn_enabled: false
